<% layout('layouts/boilerplate')%>
 
   
   

<div class="body_div userorders">
               
    <% for( let i = orders.length-1; i>=0; i-- ) { %>
     <div class="card col-10">
         <div class="card-header">
            Order:<%= i+1 %>
         </div>
         <div class="card-body">
           
           <a href="/<%= currentuser._id %>/orders/<%= i %>" class="btn btn-info"> expand</a>
         </div>
       </div>

      
    
    <% } %>  

</div>